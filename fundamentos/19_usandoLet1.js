let numero = 1
{
    let numero = 2
    console.log('dentro =', numero)
}
console.log('fora =', numero)

// Variável definida com a palavra reservada "var", tem escopo global e de função.

// Variável definida com a palavra reservada "let", tem escopo global, escopo de função e escopo de bloco.